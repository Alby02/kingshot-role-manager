services:
  bot:
    build:
      context: .
      dockerfile: Containerfile
    image: kingshot-bot:latest
    container_name: kingshot-bot
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DB_PATH=data/kingshot.db
